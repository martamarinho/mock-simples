{
  "name": "ninja2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
   "start:mock": "json-server --watch db.json --port 3000",
  "test:ci": "cypress run",
  "ci": "start-server-and-test start:mock http://localhost:3000 test:ci",
  "test": "npm run test:ci"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^15.9.0",
    "json-server": "^0.17.3",
    "open": "^11.0.0"
  },
  "devDependencies": {
    "start-server-and-test": "^2.1.3",
    "wait-on": "^9.0.4"
  }
}
